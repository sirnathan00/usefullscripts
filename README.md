# usefullscripts
own personal usefull scripts don't use unless you want to install unknown accounts to your own server

to call the setup_proxmox script use

```bash -c "$(curl -fsSL https://raw.githubusercontent.com/sirnathan00/usefullscripts/refs/heads/main/Setup_proxmox.sh)"```